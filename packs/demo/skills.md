# Learned Skills

_Seed file — no runs distilled yet. `agenta.py learn demo` will overwrite this
once there are executed runs in `runs.jsonl`._

- Lead with a one-sentence "why this matters" hook.
- Prefer one worked example over three abstract definitions.
